<%= @comment.errors.full_messages.join(", ") %> <br>

<%= simple_form_for [@blog, @comment], remote: true do |f| %>
 <%= f.input :body, hint: "enter a comment..." %>
 <%= f.submit({class: "btn btn-primary"}) %>
<% end %>
